<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cardiac.general.hospital.medicare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenu"
            android:label="Main Menu" />
        <activity android:name=".Specialities"
            android:label="Specialities" />
        <activity android:name=".FindConsultant"
            android:label="Find a Consultant" />
        <activity android:name=".AddAppointment"
            android:label="AddAppointment" />
        <activity android:name=".OnlineReport"
            android:label="Online Reports" />
        <activity android:name=".About"
            android:label="About us"  />
        <activity android:name=".Contact"
            android:label="Contact us"/>
    </application>

</manifest>